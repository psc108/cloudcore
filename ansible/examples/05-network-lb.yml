---
# 05-network-lb.yml
# VPC + two instances + a network (L4) load balancer for TCP traffic.
# Use this pattern for non-HTTP workloads (databases, game servers, etc.).
# Run: ansible-playbook -i ../inventory.ini 05-network-lb.yml
- name: "Example 05 — Network load balancer (L4 NLB)"
  hosts: localhost
  gather_facts: false

  vars:
    env: "{{ default_environment }}"
    project: "{{ default_project }}"
    vpc_name: "{{ project }}-{{ env }}-{{ build_suffix }}-main"
    lb_name: "{{ project }}-{{ env }}-{{ build_suffix }}-nlb"
    backend_port: 5432   # example: PostgreSQL
    # Leave blank to keep both db instances local (the default --
    # everything below is a no-op). To put db-02 on a paired remote host
    # instead, set db_02_peer_id (see the Dashboard's Peers section, or
    # the peer_info Ansible module, for available hosts) AND
    # db_02_peer_vpc_id/db_02_peer_subnet_id to THAT peer's own
    # vpc_id/subnet_id. Those two aren't optional once peer_id is set: a
    # remote peer has its own separate VPC/subnet catalogue, not this
    # build's local one (see haFullStack-LLD.md §13, and
    # modules/instance-group's own placement_overrides for the same
    # requirement in the Terraform world).
    db_02_peer_id: ""
    db_02_peer_vpc_id: ""
    db_02_peer_subnet_id: ""
    instances:
      - name: "{{ project }}-{{ env }}-{{ build_suffix }}-db-01"
        subnet_id: subnet-local-01
        peer_id: ""
        vpc_id_override: ""
      - name: "{{ project }}-{{ env }}-{{ build_suffix }}-db-02"
        subnet_id: "{{ db_02_peer_subnet_id | default('subnet-local-01', true) }}"
        peer_id: "{{ db_02_peer_id }}"
        vpc_id_override: "{{ db_02_peer_vpc_id }}"

  tasks:
    - name: Create VPC
      cloudcore.cloudcore.vpc:
        api_url: "{{ cloudcore_api_url }}"
        api_token: "{{ cloudcore_api_token }}"
        name: "{{ vpc_name }}"
        cidr_block: "10.20.0.0/16"
        dns_support: true
        tags:
          Environment: "{{ env }}"
          Project: "{{ project }}"
          Owner: "{{ default_owner }}"
          ManagedBy: ansible
      register: vpc

    - name: Create backend instances
      cloudcore.cloudcore.instance:
        api_url: "{{ cloudcore_api_url }}"
        api_token: "{{ cloudcore_api_token }}"
        name: "{{ item.name }}"
        image_id: ubuntu-22.04
        flavor: standard.medium
        vpc_id: "{{ item.vpc_id_override | default(vpc.vpc.id, true) }}"
        subnet_id: "{{ item.subnet_id }}"
        peer_id: "{{ item.peer_id }}"
        user_data: |
          #cloud-config
          # apt_preserve_sources_list + bootcmd point this guest at the
          # host-level package repo (api/build-package-repo.sh,
          # haFullStack.md §14) exclusively, the same pattern every
          # other example uses — promtail is only ever cached there,
          # not in the real Ubuntu archive.
          apt_preserve_sources_list: true
          bootcmd:
            - |
              cat > /etc/apt/sources.list <<'EOF'
              deb [trusted=yes] http://192.168.100.1:8090/jammy/apt-repo ./
              EOF

          package_update: true
          packages:
            - promtail

          write_files:
            # Centralized logging (haFullStack-LLD.md §12) — ships
            # this node's own journal + cloud-init-output.log to the
            # host-level Loki service. __HOSTNAME__ fixed up in
            # runcmd below, same sed-substitution idiom this platform
            # already uses elsewhere.
            - path: /etc/promtail/config.yml
              content: |
                server:
                  http_listen_port: 9080
                  grpc_listen_port: 0

                positions:
                  filename: /tmp/positions.yaml

                clients:
                  # Fixed address, not a discovered/templated one — Loki is a
                  # host-level, always-on service (api/setup-logging-service.sh,
                  # haFullStack-LLD.md §12), the same platform-capability pattern as
                  # the host-level package repo (192.168.100.1:8090, §7) rather than a
                  # dedicated per-example node. Every example's own promtail ships
                  # here, which is what makes log coverage apply no matter which
                  # example gets built, not just this one.
                  - url: http://192.168.100.1:3100/loki/api/v1/push

                # __HOSTNAME__ is fixed up at boot (this node's real hostname isn't
                # known until runtime) via the same sed-substitution idiom this stack
                # already uses for frontend's own index.html (__HOSTNAME__) and
                # render-keepalived-conf.sh's __IFACE__ — see each tier's own
                # cloud-init runcmd for the caller.
                scrape_configs:
                  # journald already captures every systemd-managed service on this
                  # node (nginx, mysql, apache2, rabbitmq-server, keepalived, proxysql,
                  # memcached, every *-status.timer, step-renew, quorum-watchdog) with
                  # zero per-service enumeration needed.
                  - job_name: journal
                    journal:
                      max_age: 12h
                      labels:
                        job: journal
                        host: __HOSTNAME__
                    relabel_configs:
                      - source_labels: ['__journal__systemd_unit']
                        target_label: 'unit'

                  # Not journal-backed the same way — and the single most useful file
                  # this whole project's own debugging has repeatedly reached for (most
                  # of haFullStack-Findings-Log.md's real findings were first spotted
                  # here, not in journalctl).
                  - job_name: cloud-init-output
                    static_configs:
                      - targets:
                          - localhost
                        labels:
                          job: cloud-init-output
                          host: __HOSTNAME__
                          __path__: /var/log/cloud-init-output.log

                  # Per-service log FILES, not just what each service's own systemd
                  # unit happens to emit to stdout/stderr (journal, above) — a real
                  # gap noticed only once someone asked "are we watching everything?"
                  # directly. One shared config applied to every tier alike (same as
                  # every scrape_config above) rather than a per-tier variant — a path
                  # that doesn't exist on a given node (e.g. the nginx target on a
                  # MySQL node) is a harmless no-op for promtail's own file-target
                  # discovery, not an error, so there's no need to fork this file per
                  # service. /var/lib/proxysql is intentionally NOT glob-matched
                  # (*.log only) — that directory also holds this node's own TLS
                  # private key material (proxysql-key.pem); see
                  # proxysql-cloud-init.yaml.tftpl's own setfacl step for exactly how
                  # promtail is granted access to proxysql.log specifically without
                  # touching those files' own permissions at all.
                  - job_name: nginx
                    static_configs:
                      - targets: [localhost]
                        labels:
                          job: nginx
                          host: __HOSTNAME__
                          __path__: /var/log/nginx/*.log

                  - job_name: mysql
                    static_configs:
                      - targets: [localhost]
                        labels:
                          job: mysql
                          host: __HOSTNAME__
                          __path__: /var/log/mysql/error.log

                  # rabbitmq's own log files are group-owned "rabbitmq", not "adm" —
                  # confirmed live (real "permission denied" until promtail was also
                  # added to that group; see rabbitmq-cloud-init.yaml.tftpl's own
                  # runcmd).
                  - job_name: rabbitmq
                    static_configs:
                      - targets: [localhost]
                        labels:
                          job: rabbitmq
                          host: __HOSTNAME__
                          __path__: /var/log/rabbitmq/*.log

                  - job_name: apache2
                    static_configs:
                      - targets: [localhost]
                        labels:
                          job: apache2
                          host: __HOSTNAME__
                          __path__: /var/log/apache2/*.log

                  - job_name: proxysql
                    static_configs:
                      - targets: [localhost]
                        labels:
                          job: proxysql
                          host: __HOSTNAME__
                          __path__: /var/lib/proxysql/proxysql.log

          runcmd:
            # F-073/F-072 promtail fix (F-076,
            # haFullStack-Findings-Log.md — promtail's own package
            # postinst auto-starts it immediately once installed,
            # before this runcmd block gets a chance to fix up
            # __HOSTNAME__ or grant the "adm" group needed to read
            # root:adm 0640 /var/log/cloud-init-output.log).
            - systemctl stop promtail || true
            - sed "s/__HOSTNAME__/$(hostname)/" -i /etc/promtail/config.yml
            - usermod -aG adm promtail
            - systemctl enable --now promtail
        tags:
          Environment: "{{ env }}"
          Project: "{{ project }}"
          Owner: "{{ default_owner }}"
          ManagedBy: ansible
          Role: database
      loop: "{{ instances }}"
      register: created_instances

    - name: Wait for all instances to reach running
      ansible.builtin.uri:
        url: "{{ cloudcore_api_url }}/v1/instances/{{ item.instance.id }}"
        headers:
          Authorization: "Bearer {{ cloudcore_api_token }}"
      register: poll
      until: poll.json.status == "running"
      retries: 30
      delay: 5
      loop: "{{ created_instances.results }}"
      loop_control:
        label: "{{ item.instance.name }}"

    - name: Create network load balancer
      cloudcore.cloudcore.load_balancer:
        api_url: "{{ cloudcore_api_url }}"
        api_token: "{{ cloudcore_api_token }}"
        name: "{{ lb_name }}"
        type: network
        vpc_id: "{{ vpc.vpc.id }}"
        subnet_ids:
          - subnet-local-01
        internal: true
        tags:
          Environment: "{{ env }}"
          Project: "{{ project }}"
          Owner: "{{ default_owner }}"
          ManagedBy: ansible
          Role: database-lb
      register: lb

    - name: Register instances as backends
      ansible.builtin.uri:
        url: "{{ cloudcore_api_url }}/v1/load-balancers/{{ lb.load_balancer.id }}/backends"
        method: POST
        headers:
          Authorization: "Bearer {{ cloudcore_api_token }}"
          Content-Type: application/json
        body_format: json
        body:
          name: "{{ item.json.name }}"
          address: "{{ item.json.private_ip }}"
          port: "{{ backend_port }}"
        status_code: 201
      loop: "{{ poll.results }}"
      loop_control:
        label: "{{ item.json.name }}"

    - name: Show NLB
      ansible.builtin.debug:
        msg: >
          NLB {{ lb.load_balancer.name }} listen_port={{ lb.load_balancer.listen_port }}
          (internal, backends on port {{ backend_port }})
